# Virtual environment
venv/

# Environment variables (NEVER commit these)
.env

# Python cache
__pycache__/
*.pyc

# IDE
.vscode/

# Photo intake folders (don't commit your bottle photos)
data/photos_to_process/
data/photos_processed/
data/photos_failed/

# Debug HTML dumps from scraper testing
*_dump.html

# Local SQLite data store — rebuildable from data/*.csv.bak via migrate_to_sqlite.py.
# Kept out of git this pass to avoid binary churn on every pipeline run. Revisit
# once the .csv.bak fallbacks are removed and the DB becomes the sole source.
data/bourbon.db

# Junk: a captured `git diff` accidentally saved as a file named ".csv"
# (root-anchored so it never matches data/*.csv)
/.csv

# Test scripts (kept locally, not part of project)
test_api.py
test_scraper.py

# OS junk
.DS_Store
Thumbs.db
# Google API credentials (never commit)
google_credentials.json
token.json